@page "/update-invoice-accounting/{id:int}"
@model UpdateInvoiceAccountingModel
@{
}

@section Meta {
    <title>
        Kontera faktura -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

@await Html.PartialAsync("/Pages/Clients/_Shared/_Invoice.cshtml", Model.UserToken)

<main>
    <h4>
        Kontera faktura
        <span class="muted">@(Model.Invoice.InvoiceNumber)</span>
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.Id" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Skapa kontering
        </button>

        <a class="button" href="/show-invoice/@(Model.Invoice.Id)">
            Avbryt
        </a>
    </form>

</main>
