@page "/set-email-account-password/{id:int}"
@model SetEmailAccountPasswordModel
@{
}

@section Meta {
    <title>
        Sätt lösenord för epostkonto -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Sätt lösenord för epostkonto:
        @(Model.EmailAccount.FromAddress)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.Id" />

        <label asp-for="CommandModel.Password">
            Lösenord
        </label>
        <input asp-for="CommandModel.Password" />

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Spara
        </button>

        <a class="button" href="/show-email-account/@(Model.EmailAccount.Id)">
            Avbryt
        </a>
    </form>

</main>
