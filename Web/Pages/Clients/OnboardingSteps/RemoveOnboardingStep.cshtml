@page "/remove-onboarding-step/{id:int}"
@model RemoveOnboardingStepModel
@{
}

@section Meta {
    <title>
        Ta bort onboarding-steg -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort onboarding-steg:
        @(Model.OnboardingStep.OnboardingStepKind)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.Id" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort
        </button>

        <a class="button" href="/show-onboarding-step/@(Model.CommandModel.Id)">
            Avbryt
        </a>
    </form>

</main>
