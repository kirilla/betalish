@page "/strip-user-admin/{id:int}"
@model StripUserAdminModel
@{
}

@section Meta {
    <title>
        Ta bort som admin -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_NavbarAdmin" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort @(Model.User.Name) admin?
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.UserId" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort som admin
        </button>

        <a class="button" href="/show-user/@(Model.CommandModel.UserId)">
            Avbryt
        </a>

    </form>

</main>
