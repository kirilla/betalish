@page "/show-client-users"
@model ShowClientUsersModel
@{
}

@section Meta {
    <title>
        Klient-användare -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Klient-användare
    </h4>

    <div class="notice">
        <ul>
            @foreach (var user in Model.Users)
            {
                <li>
                    👤
                    &thinsp;
                    <a href="/show-user/@(user.Id)">
                        @(user.Name)
                    </a>
                </li>
            }

            @if (!Model.Users.Any())
            {
                <li>
                    👤
                    &thinsp;
                    &ndash;
                </li>
            }
        </ul>
    </div>

</main>
